document.addEventListener('click',event=>{const button=event.target.closest('[data-collapse-nav] [data-toggle]');if(!button)return;const nav=button.closest('[data-collapse-nav]');const breakpoint=nav.classList.contains('ui-collapse-nav--attendance')?'(max-width: 900px)':'(max-width: 991.98px)';if(matchMedia(breakpoint).matches)return;const collapsed=!nav.classList.contains('is-collapsed'),expanded=nav.querySelector('[data-expanded]'),compact=nav.querySelector('[data-compact]');nav.classList.toggle('is-collapsed',collapsed);expanded?.toggleAttribute('inert',collapsed);expanded?.setAttribute('aria-hidden',String(collapsed));compact?.toggleAttribute('inert',!collapsed);compact?.setAttribute('aria-hidden',String(!collapsed));nav.querySelectorAll('[data-toggle]').forEach(item=>item.setAttribute('aria-expanded',String(!collapsed)));nav.querySelector(collapsed?'[data-compact] [data-toggle]':'[data-expanded] [data-toggle]')?.focus()});
